import 'package:flutter/material.dart';

class ProfileCreatePage extends StatelessWidget {
  const ProfileCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Profil yaratish"),
      ),
    );
  }
}
